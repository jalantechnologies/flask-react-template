name: ci

on:
  pull_request:
    types: [ opened, synchronize, reopened ]

permissions:
  contents: read
  pull-requests: write

jobs:
  lint:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.13.1'
          cache: 'npm'

      - name: Install pipenv
        run: pip install pipenv

      - name: Install Python dependencies
        run: pipenv install --dev --deploy

      - name: Install Node dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

  sonarqube:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Get github-ci version
        id: ci_version
        run: |
          # Extract version from cd.yml workflow
          workflow_yaml=$(cat .github/workflows/cd.yml)
          uses_data=$(echo "$workflow_yaml" | grep 'jalantechnologies/github-ci')
          tag=$(echo $uses_data | sed -n 's/.*@\(.*\)/\1/p')
          echo "version=$(echo $tag)" >> $GITHUB_OUTPUT

      - name: Checkout github-ci
        uses: actions/checkout@v3
        with:
          repository: jalantechnologies/github-ci
          path: platform
          ref: ${{ steps.ci_version.outputs.version }}

      - name: Run SonarQube analysis
        uses: ./platform/.github/actions/analyze
        with:
          sonar_host_url: ${{ vars.SONAR_HOST_URL }}
          sonar_token: ${{ secrets.SONAR_TOKEN }}
          branch: ${{ github.head_ref }}
          branch_base: main
          pull_request_number: ${{ github.event.number }}

  review:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder for code review
        run: |
          echo "Code review placeholder"
          echo "Future implementation: AI-powered review for architecture, security, and best practices"

  security:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run dependency review
        id: review
        continue-on-error: true
        uses: actions/dependency-review-action@v4
        with:
          comment-summary-in-pr: always
          vulnerability-check: true

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc

      - name: Dependency review result
        id: deps
        run: |
          echo "Dependency review completed. See above for details."
          echo "Vulnerabilities found: ${{ steps.review.outputs.comment-content }}"
          echo "${{ steps.review.outputs.comment-content}}" > input.html
          sed -e '/<h2>OpenSSF Scorecard<\/h2>/,/<h2>/{
            /<h2>OpenSSF Scorecard<\/h2>/!{
              /<h2>/!d
            }
          }' \
          -e '/<h2>Scanned Files<\/h2>/,/<h2>/{
            /<h2>Scanned Files<\/h2>/!{
              /<h2>/!d
            }
          }' input.html > filtered.html || cp input.html filtered.html

          pandoc -f html -t gfm -o deps-summary.md filtered.html
          # echo "${{ steps.review.outputs.vulnerable-changes}}" >> dependency.json 
          # # Create Markdown header
          # echo "### 🧩 Dependency Review Summary" > deps-summary.md
          # echo "" >> deps-summary.md
          # echo "| Change Type | Package | Version | Severity | Details |" >> deps-summary.md
          # echo "|--------------|----------|----------|-----------|----------|" >> deps-summary.md

          # # Append rows
          # jq -r '
          #   .[]
          #   | select(any(.vulnerabilities[]?; .severity == "high" or .severity == "critical"))
          #   | "| \(.change_type) | \(.name) | \(.version // "-") | \([.vulnerabilities[]? | select(.severity == "high" or .severity == "critical") | .severity] | join(", ")) | \([.vulnerabilities[]? | select(.severity == "high" or .severity == "critical") | .advisory_summary] | join(" / ")) |"
          # ' dependency.json >> deps-summary.md

          cat deps-summary.md
          cat deps-summary.md > TEMP
          echo $TEMP >> $GITHUB_OUTPUT


      - name: Comment PR with audit results
        continue-on-error: true
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          recreate: true
          path: deps-summary.md

      # - name: Install jq for JSON parsing
      #   run: sudo apt-get update && sudo apt-get install -y jq

      # - name: Detect changed dependencies in package.json
      #   id: deps
      #   run: |
      #     BASE_BRANCH=${{ github.event.pull_request.base.ref }}
      #     echo "Comparing current branch with $BASE_BRANCH..."

      #     git fetch origin $BASE_BRANCH

      #     # Extract dependency differences
      #     DIFF=$(git diff origin/$BASE_BRANCH -- package.json | grep '^[+-]\s*"[^"]*":' || true)

      #     echo "Dependency changes:"
      #     CHANGED_PKGS_JSON=$(echo "$DIFF" | sed -E 's/^[+-]\s*"([^"]*)".*/"\1"/' | sort -u | jq -s '.')
      #     echo "changed_packages=$CHANGED_PKGS_JSON" >> $GITHUB_OUTPUT

      #     # CHANGED_PKGS=$(echo "$DIFF" | sed -E 's/^[+-]\s*"([^"]*)".*/\1/' | sort -u)
      #     # echo "Changed packages: $CHANGED_PKGS"

      #     echo "changed_packages=$CHANGED_PKGS" >> $GITHUB_OUTPUT


      # - name: Run npm audit for changed dependencies
      #   if: steps.deps.outputs.changed_packages != ''
      #   run: |
      #     npm ci
      #     npm audit --json > audit.json || true
          
      
      # - name: Generate Markdown table for vulnerabilities
      #   if: steps.deps.outputs.changed_packages != ''
      #   id: table
      #   run: |
      #     CHANGED_PKGS="${{ steps.deps.outputs.changed_packages }}"
          
      #     echo "### ⚠️ Vulnerabilities in changed dependencies" > audit_table.md
      #     echo "| Package | Severity | Title | Affected Versions | Fix Available | Advisory |" >> audit_table.md
      #     echo "|---------|---------|-------|-----------------|---------------|---------|" >> audit_table.md

      #     for PKG in $CHANGED_PKGS; do
      #       jq -c --arg PKG "$PKG" '.vulnerabilities[$PKG] // empty | .via[] | select(.name==$PKG)' audit.json | while read vuln; do
      #         TITLE=$(echo "$vuln" | jq -r '.title')
      #         SEV=$(echo "$vuln" | jq -r '.severity')
      #         RANGE=$(echo "$vuln" | jq -r '.range')
      #         FIX=$(echo "$vuln" | jq -r 'if .fixAvailable then "Yes" else "No" end')
      #         URL=$(echo "$vuln" | jq -r '.url')
      #         echo "| $PKG | $SEV | $TITLE | $RANGE | $FIX | [$URL]($URL) |" >> audit_table.md
      #       done
      #     done

      #     cat audit_table.md
      #     cat audit_table.md > TEMP
      #     echo $TEMP >> $GITHUB_OUTPUT

      # - name: Comment PR with audit results
      #   if: steps.deps.outputs.changed_packages != ''
      #   continue-on-error: true
      #   uses: marocchino/sticky-pull-request-comment@v2
      #   with:
      #     recreate: true
      #     path: audit_table.md

          
  test:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run integration tests
        run: docker compose -f docker-compose.test.yml up --exit-code-from app

      - name: Coverage report
        continue-on-error: true
        uses: irongut/CodeCoverageSummary@v1.3.0
        with:
          filename: output/coverage.xml
          badge: true
          fail_below_min: true
          format: markdown
          hide_branch_rate: false
          hide_complexity: true
          indicators: true
          output: both
          thresholds: '60 80'

      - name: Add coverage PR comment
        continue-on-error: true
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          recreate: true
          path: code-coverage-results.md
