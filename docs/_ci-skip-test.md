# CI Skip Test

This is a throwaway document used to confirm that a documentation-only pull request
skips the heavy CI checks (`test-backend`, `test-frontend`, `scan`, `sonarqube`) while
still running `lint`, `label`, and `codereview`.

Delete this file and close the pull request once the behaviour is confirmed.
